"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.computePieLayout = void 0;
function computePieLayout({ canvasSize, size, }) {
    const layoutWidth = size !== null && size !== void 0 ? size : canvasSize.width;
    const layoutHeight = size !== null && size !== void 0 ? size : canvasSize.height;
    const radius = Math.min(layoutWidth, layoutHeight) / 2;
    const centerX = (canvasSize.width - layoutWidth) / 2 + layoutWidth / 2;
    const centerY = (canvasSize.height - layoutHeight) / 2 + layoutHeight / 2;
    return { centerX, centerY, radius, layoutWidth, layoutHeight };
}
exports.computePieLayout = computePieLayout;
