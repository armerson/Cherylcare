import { type ChartCanvasSize } from "../../shared/chartCanvasSizeUtils";
export type ComputedPieLayout = {
    centerX: number;
    centerY: number;
    radius: number;
    layoutWidth: number;
    layoutHeight: number;
};
export declare function computePieLayout({ canvasSize, size, }: {
    canvasSize: ChartCanvasSize;
    size?: number;
}): ComputedPieLayout;
