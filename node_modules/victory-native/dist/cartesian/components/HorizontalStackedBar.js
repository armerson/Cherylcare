"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.HorizontalStackedBar = void 0;
const React = __importStar(require("react"));
const react_native_skia_1 = require("@shopify/react-native-skia");
const AnimatedPath_1 = require("./AnimatedPath");
const CartesianChartContext_1 = require("../contexts/CartesianChartContext");
const useHorizontalStackedBarPaths_1 = require("../hooks/useHorizontalStackedBarPaths");
const DEFAULT_COLORS = ["red", "orange", "blue", "green", "blue", "purple"];
const HorizontalStackedBar = ({ points, chartBounds, animate, innerPadding = 0.25, barWidth, barCount, barOptions, colors = DEFAULT_COLORS, }) => {
    const { orientation } = (0, CartesianChartContext_1.useCartesianChartContext)();
    const paths = (0, useHorizontalStackedBarPaths_1.useHorizontalStackedBarPaths)({
        points,
        chartBounds,
        innerPadding,
        barWidth,
        barCount,
        barOptions,
        colors,
    });
    if (orientation !== "horizontal") {
        if (process.env.NODE_ENV !== "production") {
            console.warn('HorizontalStackedBar must be rendered inside a CartesianChart with orientation="horizontal".');
        }
        return null;
    }
    return (<>
      {paths.map((p) => {
            return React.createElement(animate ? AnimatedPath_1.AnimatedPath : react_native_skia_1.Path, Object.assign(Object.assign(Object.assign({}, p), { style: "fill" }), (Boolean(animate) && { animate })));
        })}
    </>);
};
exports.HorizontalStackedBar = HorizontalStackedBar;
