export declare const getXScaleInputBounds: ({ isNumericalData, ixNum, domain, tickDomain, }: {
    isNumericalData: boolean;
    ixNum: number[];
    domain?: [number] | [number, number];
    tickDomain?: [number, number];
}) => [number, number];
