import type { ChartBounds } from "../../types";
export type GetBarGroupDimensionsArgs = {
    chartBounds: Pick<ChartBounds, "left" | "right">;
    betweenGroupPadding: number;
    withinGroupPadding: number;
    groupCount: number;
    barsPerGroup: number;
    customBarWidth?: number;
    barCount?: number;
};
export declare const getBarGroupDimensions: ({ chartBounds, betweenGroupPadding, withinGroupPadding, groupCount, barsPerGroup, customBarWidth, barCount, }: GetBarGroupDimensionsArgs) => {
    barWidth: number;
    groupWidth: number;
    gapWidth: number;
};
