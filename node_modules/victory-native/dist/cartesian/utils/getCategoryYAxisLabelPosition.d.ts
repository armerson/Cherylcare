import type { AxisLabelPosition, ChartBounds, YAxisSide } from "../../types";
export declare const getCategoryYAxisLabelPosition: ({ axisSide, labelPosition, labelOffset, labelWidth, lineCount, lineHeight, fontSize, tickPosition, chartBounds, }: {
    axisSide: YAxisSide;
    labelPosition: AxisLabelPosition;
    labelOffset: number;
    labelWidth: number;
    lineCount: number;
    lineHeight: number;
    fontSize: number;
    tickPosition: number;
    chartBounds: ChartBounds;
}) => {
    x: number;
    y: number;
    canFitContent: boolean;
};
