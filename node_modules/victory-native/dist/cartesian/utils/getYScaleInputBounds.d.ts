import type { YAxisDomain } from "../../types";
export declare const getYScaleInputBounds: ({ data, yKeys, domain, tickDomain, }: {
    data: Record<string, unknown>[];
    yKeys: string[];
    domain?: YAxisDomain;
    tickDomain?: YAxisDomain;
}) => {
    yMin: number;
    yMax: number;
};
