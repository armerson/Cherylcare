import type { AxisScaleType } from "../../types";
export declare const getYScaleDomain: ({ yMin, yMax, yAxisScale, }: {
    yMin: number;
    yMax: number;
    yAxisScale?: AxisScaleType;
}) => [number, number];
