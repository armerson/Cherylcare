import type { PointsArray } from "../../types";
import type { HorizontalBarRect } from "./getHorizontalBarRect";
type Point = PointsArray[number];
export declare const getHorizontalBarGroupRect: ({ point, baselineX, barWidth, groupWidth, gapWidth, barIndex, }: {
    point: Point;
    baselineX: number;
    barWidth: number;
    groupWidth: number;
    gapWidth: number;
    barIndex: number;
}) => HorizontalBarRect | null;
export {};
