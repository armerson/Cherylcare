import type { CartesianChartOrientation, ChartBounds, SidedNumber, Viewport } from "../../types";
type CartesianScale = {
    (value: number): number;
    domain: () => number[];
};
export declare const getCartesianChartBounds: ({ xScale, yScale, viewport, domainPadding, orientation, }: {
    xScale: CartesianScale;
    yScale: CartesianScale;
    viewport?: Viewport;
    domainPadding?: SidedNumber;
    orientation?: CartesianChartOrientation;
}) => ChartBounds;
export {};
